{
  "private": true,
  "version": "0.0.0",
  "name": "example-getting-started",
  "devDependencies": {
    "babel-preset-env": "*",
    "jest": "*"
  },
  "setupFiles": "test-env.js",
  "scripts": {
    "build": "babel src -d lib",
    "test": "jest"
  }
}
